<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE html>
<html b:version='2' class='v2' expr:dir='data:blog.languageDirection' xmlns='http://www.w3.org/1999/xhtml' xmlns:b='http://www.google.com/2005/gml/b' xmlns:data='http://www.google.com/2005/gml/data' xmlns:expr='http://www.google.com/2005/gml/expr'>
<head>
  <meta charset='UTF-8'/>
  <meta content='width=device-width, initial-scale=1' name='viewport'/>
  <b:include data='blog' name='all-head-content'/>
  <title><data:blog.pageTitle/></title>
  <link href='https://fonts.googleapis.com/css2?family=Orbitron:wght@700;900&amp;family=Rajdhani:wght@400;600&amp;display=swap' rel='stylesheet'/>
  <b:skin><![CDATA[
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --pink: #ff2d78;
      --cyan: #00f7ff;
      --purple: #b400ff;
      --dark: #04000f;
      --glass: rgba(255,255,255,0.04);
    }

    html, body {
      min-height: 100vh;
      background: var(--dark) !important;
      font-family: 'Rajdhani', sans-serif;
      overflow-x: hidden;
    }

    /* Hide all default Blogger elements */
    #header-wrapper, #header, .header-wrapper,
    #navbar, .navbar, #navBar,
    #footer, .footer, #footer-wrapper,
    #sidebar, .sidebar, #sidebar-wrapper,
    .blog-pager, #blog-pager,
    #Attribution1, .attribution,
    .post-body, .post-title, .post-footer,
    .post-header, .post-labels,
    .share-buttons, .comments, #comments,
    .widget-title, .widget-content,
    #main-wrapper > .widget,
    .blog-feeds { display: none !important; }

    #outer-wrapper, #main-wrapper, #main,
    .outer-wrapper, .main-wrapper, .main,
    #content-wrapper, .content-wrapper {
      background: transparent !important;
      border: none !important;
      padding: 0 !important;
      margin: 0 auto !important;
      max-width: 100% !important;
      float: none !important;
      width: 100% !important;
      box-shadow: none !important;
    }

    body::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image:
        linear-gradient(rgba(0,247,255,0.04) 1px, transparent 1px),
        linear-gradient(90deg, rgba(0,247,255,0.04) 1px, transparent 1px);
      background-size: 40px 40px;
      animation: gridMove 20s linear infinite;
      pointer-events: none;
      z-index: 0;
    }

    body::after {
      content: '';
      position: fixed;
      inset: 0;
      background:
        radial-gradient(ellipse 60% 50% at 20% 20%, rgba(180,0,255,0.15) 0%, transparent 60%),
        radial-gradient(ellipse 50% 40% at 80% 80%, rgba(255,45,120,0.15) 0%, transparent 60%),
        radial-gradient(ellipse 40% 60% at 50% 50%, rgba(0,247,255,0.07) 0%, transparent 70%);
      pointer-events: none;
      z-index: 0;
    }

    @keyframes gridMove {
      0% { transform: translateY(0); }
      100% { transform: translateY(40px); }
    }

    .tt-page {
      position: relative;
      z-index: 1;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 40px 20px;
    }

    .tt-container {
      position: relative;
      z-index: 1;
      width: 100%;
      max-width: 420px;
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 16px;
    }

    .tt-logo-box {
      position: relative;
      padding: 28px 48px;
      margin-bottom: 10px;
      text-align: center;
      animation: fadeDown 0.6s ease both;
    }

    .tt-logo-box::before {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: 16px;
      border: 1.5px solid transparent;
      background: linear-gradient(135deg, var(--cyan), var(--pink), var(--purple)) border-box;
      -webkit-mask: linear-gradient(#fff 0 0) padding-box, linear-gradient(#fff 0 0);
      -webkit-mask-composite: destination-out;
      mask-composite: exclude;
      animation: borderGlow 3s ease-in-out infinite alternate;
    }

    .tt-logo-box::after {
      content: '';
      position: absolute;
      inset: -2px;
      border-radius: 18px;
      background: linear-gradient(135deg, var(--cyan), var(--pink), var(--purple));
      filter: blur(18px);
      opacity: 0.25;
      z-index: -1;
      animation: borderGlow 3s ease-in-out infinite alternate;
    }

    @keyframes borderGlow {
      0% { opacity: 0.2; filter: blur(14px); }
      100% { opacity: 0.45; filter: blur(22px); }
    }

    .tt-logo-icon {
      font-size: 2rem;
      margin-bottom: 4px;
      display: block;
      animation: iconPulse 2s ease-in-out infinite;
    }

    @keyframes iconPulse {
      0%, 100% { transform: scale(1); }
      50% { transform: scale(1.1); }
    }

    .tt-logo-title {
      font-family: 'Orbitron', sans-serif;
      font-size: 1.6rem;
      font-weight: 900;
      letter-spacing: 0.08em;
      background: linear-gradient(90deg, var(--cyan), #fff, var(--pink));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .tt-glow-text {
      font-family: 'Orbitron', sans-serif;
      font-size: 0.7rem;
      letter-spacing: 0.3em;
      text-transform: uppercase;
      color: var(--cyan);
      margin-top: 6px;
      animation: textFlicker 4s infinite;
      filter: drop-shadow(0 0 6px var(--cyan));
    }

    @keyframes textFlicker {
      0%, 94%, 96%, 100% { opacity: 1; }
      95% { opacity: 0.5; }
    }

    .tt-btn {
      position: relative;
      display: flex;
      align-items: center;
      gap: 14px;
      width: 100%;
      padding: 15px 22px;
      text-decoration: none !important;
      color: white !important;
      font-family: 'Rajdhani', sans-serif;
      font-size: 1rem;
      font-weight: 600;
      letter-spacing: 0.06em;
      text-transform: uppercase;
      border-radius: 12px;
      background: var(--glass);
      backdrop-filter: blur(12px);
      border: 1px solid rgba(255,255,255,0.08);
      overflow: hidden;
      transition: all 0.35s cubic-bezier(0.23, 1, 0.32, 1);
      cursor: pointer;
      animation: slideIn 0.5s ease both;
    }

    .tt-btn::before {
      content: '';
      position: absolute;
      inset: 0;
      opacity: 0;
      transition: opacity 0.35s;
      border-radius: 12px;
    }

    .tt-btn::after {
      content: '';
      position: absolute;
      left: -60%;
      top: -50%;
      width: 30%;
      height: 200%;
      background: rgba(255,255,255,0.08);
      transform: skewX(-20deg);
      transition: left 0.6s ease;
    }

    .tt-btn:hover::after { left: 130%; }

    .tt-btn:hover {
      transform: translateY(-3px) scale(1.02);
      border-color: transparent;
    }

    .tt-btn-1::before { background: linear-gradient(120deg, #ff0050, #ff2d78); }
    .tt-btn-1 .tt-icon { color: #ff2d78; filter: drop-shadow(0 0 8px #ff2d78); }
    .tt-btn-1:hover { box-shadow: 0 0 24px rgba(255,45,120,0.5), 0 8px 32px rgba(255,45,120,0.2); border-color: rgba(255,45,120,0.6); }
    .tt-btn-1:hover::before { opacity: 0.15; }

    .tt-btn-2::before { background: linear-gradient(120deg, #f5003e, #ff6a00); }
    .tt-btn-2 .tt-icon { color: #ff6a00; filter: drop-shadow(0 0 8px #ff6a00); }
    .tt-btn-2:hover { box-shadow: 0 0 24px rgba(255,106,0,0.5), 0 8px 32px rgba(255,106,0,0.2); border-color: rgba(255,106,0,0.6); }
    .tt-btn-2:hover::before { opacity: 0.15; }

    .tt-btn-3::before { background: linear-gradient(120deg, #00c6ff, #0072ff); }
    .tt-btn-3 .tt-icon { color: #00f7ff; filter: drop-shadow(0 0 8px #00f7ff); }
    .tt-btn-3:hover { box-shadow: 0 0 24px rgba(0,247,255,0.5), 0 8px 32px rgba(0,247,255,0.2); border-color: rgba(0,247,255,0.6); }
    .tt-btn-3:hover::before { opacity: 0.15; }

    .tt-btn-4::before { background: linear-gradient(120deg, #b400ff, #7000ff); }
    .tt-btn-4 .tt-icon { color: #d466ff; filter: drop-shadow(0 0 8px #d466ff); }
    .tt-btn-4:hover { box-shadow: 0 0 24px rgba(180,0,255,0.5), 0 8px 32px rgba(180,0,255,0.2); border-color: rgba(180,0,255,0.6); }
    .tt-btn-4:hover::before { opacity: 0.15; }

    .tt-btn-5::before { background: linear-gradient(120deg, #00ff87, #00b4ff); }
    .tt-btn-5 .tt-icon { color: #00ff87; filter: drop-shadow(0 0 8px #00ff87); }
    .tt-btn-5:hover { box-shadow: 0 0 24px rgba(0,255,135,0.5), 0 8px 32px rgba(0,255,135,0.2); border-color: rgba(0,255,135,0.6); }
    .tt-btn-5:hover::before { opacity: 0.15; }

    .tt-icon { font-size: 1.3rem; flex-shrink: 0; transition: transform 0.3s ease; }
    .tt-btn:hover .tt-icon { transform: scale(1.2) rotate(-5deg); }
    .tt-label { flex: 1; text-align: left; }

    .tt-arrow {
      font-size: 1rem;
      opacity: 0;
      transform: translateX(-8px);
      transition: all 0.3s ease;
      color: rgba(255,255,255,0.6);
    }

    .tt-btn:hover .tt-arrow { opacity: 1; transform: translateX(0); }

    .tt-btn:nth-child(1) { animation-delay: 0.1s; }
    .tt-btn:nth-child(2) { animation-delay: 0.2s; }
    .tt-btn:nth-child(3) { animation-delay: 0.3s; }
    .tt-btn:nth-child(4) { animation-delay: 0.4s; }
    .tt-btn:nth-child(5) { animation-delay: 0.5s; }

    @keyframes slideIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    @keyframes fadeDown {
      from { opacity: 0; transform: translateY(-20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .tt-particle {
      position: fixed;
      border-radius: 50%;
      pointer-events: none;
      z-index: 0;
      animation: floatUp linear infinite;
    }

    @keyframes floatUp {
      0% { transform: translateY(100vh) scale(0); opacity: 0; }
      10% { opacity: 0.6; }
      90% { opacity: 0.3; }
      100% { transform: translateY(-10vh) scale(1); opacity: 0; }
    }
  ]]></b:skin>
</head>

<body>

  <script type='text/javascript'>
  //<![CDATA[
    (function() {
      var colors = ['#ff2d78','#00f7ff','#b400ff','#ff6a00','#00ff87'];
      for (var i = 0; i < 18; i++) {
        var p = document.createElement('div');
        p.className = 'tt-particle';
        var size = Math.random() * 4 + 2;
        p.style.cssText =
          'width:' + size + 'px;' +
          'height:' + size + 'px;' +
          'background:' + colors[Math.floor(Math.random() * colors.length)] + ';' +
          'left:' + (Math.random() * 100) + 'vw;' +
          'animation-duration:' + (Math.random() * 12 + 8) + 's;' +
          'animation-delay:' + (Math.random() * 10) + 's;' +
          'filter:blur(1px);' +
          'box-shadow:0 0 6px currentColor;';
        document.body.appendChild(p);
      }
    })();
  //]]>
  </script>

  <div class='tt-page'>
    <div class='tt-container'>

      <div class='tt-logo-box'>
        <span class='tt-logo-icon'>&#9835;</span>
        <div class='tt-logo-title'>TikTok Tools</div>
        <div class='tt-glow-text'>&#9889; All-in-one creator kit &#9889;</div>
      </div>

      <a class='tt-btn tt-btn-1' href='https://app.ytdown.to/en27/' target='_blank'>
        <span class='tt-icon'>&#9654;</span>
        <span class='tt-label'>YouTube Downloader</span>
        <span class='tt-arrow'>&#8594;</span>
      </a>

      <a class='tt-btn tt-btn-2' href='https://sssinstagram.com/en1' target='_blank'>
        <span class='tt-icon'>&#9672;</span>
        <span class='tt-label'>Instagram Downloader</span>
        <span class='tt-arrow'>&#8594;</span>
      </a>

      <a class='tt-btn tt-btn-3' href='https://wink.ai/video-watermark-remover' target='_blank'>
        <span class='tt-icon'>&#10022;</span>
        <span class='tt-label'>Watermark Remover</span>
        <span class='tt-arrow'>&#8594;</span>
      </a>

      <a class='tt-btn tt-btn-4' href='https://viralhook-ai-lyart.vercel.app' target='_blank'>
        <span class='tt-icon'>&#10023;</span>
        <span class='tt-label'>Magic Viral Title &amp; Hashtags</span>
        <span class='tt-arrow'>&#8594;</span>
      </a>

      <a class='tt-btn tt-btn-5' href='https://h4k3r-ai.vercel.app' target='_blank'>
        <span class='tt-icon'>&#9670;</span>
        <span class='tt-label'>AI Video Generator</span>
        <span class='tt-arrow'>&#8594;</span>
      </a>

    </div>
  </div>

  <b:section class='main' id='main' maxwidgets='1' showaddelement='no'>
    <b:widget id='Blog1' locked='true' title='Blog Posts' type='Blog'>
      <b:widget-settings>
        <b:widget-setting name='showShareButtons'>false</b:widget-setting>
        <b:widget-setting name='showCommentLink'>false</b:widget-setting>
        <b:widget-setting name='showAuthor'>false</b:widget-setting>
        <b:widget-setting name='showTimestamp'>false</b:widget-setting>
        <b:widget-setting name='showLabel'>false</b:widget-setting>
      </b:widget-settings>
      <b:includable id='main' var='top'>
      </b:includable>
    </b:widget>
  </b:section>

</body>
</html>
